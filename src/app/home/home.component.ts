import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-home',
  standalone: true,
  templateUrl: './home.component.html',
  styleUrls: ['./home.component.css'],
  imports: [CommonModule]
})
export class HomeComponent {
  // Your existing code for HomeComponent can go here, if any.

  // You can add any other logic related to the home page here, such as handling clicks on images, etc.
}
